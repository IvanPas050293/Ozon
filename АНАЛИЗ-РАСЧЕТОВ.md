# Анализ расчётной логики книги «3 Чистяковой д 2 (ИП Решеткин Иван Сергеевич).xlsx»

Дата анализа: 2026-08-10. Источник: файл-книга (9 вкладок) + CSV-выгрузка «Продажи» + банковские выписки `kl_to_1c`.

Цель: понять, как считаются формулы во всех вкладках, чтобы перенести логику в приложение, которое будет получать данные по API и считать всё автоматически.

---

## 1. Общая архитектура книги

| Вкладка | Роль | Данные | Формулы |
| --- | --- | --- | --- |
| **Продажи** | Сырые ежедневные продажи | дата (A), сумма (B), служебная дата (C) | G1 = SUM(B2:B3004) — итого |
| **Кассовый отчет** | Ежедневный кассовый учёт | приход/расход/остатки по дням | Контроль K = B + C + D − E − G − I; месячные итоги |
| **Расх** | Еженедельные расходы | ЗП, нужды, закуп/аренда, Михаил, дивиденды | Всего по колонкам; месячные блоки N:O, Q:R |
| **Комиссии** | Банковские комиссии по дням | эквайринг/QR/подписки по месяцам | ИТОГО = SUM по месяцам |
| **Расчет** | **Главный итог** — распределение прибыли | доход, зп, расходы, аренда | J = C−D−E−F−G(−H−I−K); L = окр(J); доли M:T |
| **Отчеты** | Сводка по месяцам | оборот, доход, маржа, среднее | G = C/F; H = D/C×100; C65 = SUM(Продажи!G1) |
| **ЗП** | Зарплата продавцов | дни и личные продажи | H8 = дни×1000 + 5%×продажи |
| **баланс говорово** | Взаимные долги магазинов (пусто) | — | — |
| **Справочник контрагентов** | Список поставщиков | — | — |

---

## 2. Вкладка «Продажи» — база оборота

- Колонка A — дата (серийный номер Excel), B — сумма продаж за день.
- **Оборот за период = сумма B за период.**
- G1 = `SUM(B2:B3004)` — общий итог (≈37,4 млн ₽, включая «битую» ячейку-дату B1730).
- J1 = 113 976 и K1 = «ноябрь 1с» — контрольная цифра из 1С (сверка).
- C24 = `AVERAGE(B2:B1000)` — средняя за день (служебная).

Вывод для приложения: API должен отдавать по дням «дата → сумма продаж». Оборот за месяц = сумма по датам месяца.

---

## 3. Вкладка «Кассовый отчет» — движение денег

Колонки (строки 3+): A=дата, B=остаток на начало дня, C=приход (продажи) наличные, D=приход с р/с/на карту, E=зарплата, G=хознужды, I=закуп/аренда за наличные, J=поставщик, K=остаток на конец дня, L=контроль.

**Базовое уравнение контроля (L):**
```
K (конец) = B (начало) + C + D − E − G − I
```
В строке может быть несколько под-строк на день (касса/карта/перемещения), и L суммирует соседние под-строки. Примеры формул из книги:
- `L1115 = B1115+C1115+D1115-I1115`
- `L52 (Итого за март) = B5+B6+C52+D52-E52-G52-I52`
- `C52 = SUM(C3:C51)`, `G52 = SUM(G3:G51)`, `I52 = SUM(I3:I51)`

Март 2022 (проверка): наличные продажи = 120 999 ₽, приход с р/с = 289 376 ₽, хознужды = 5 319 ₽, закуп/аренда = 334 653 ₽, зарплата = 49 571 ₽.

Вывод для приложения: вести по дням остаток и все виды приходов/расходов, контроль сходится по уравнению выше.

---

## 4. Вкладка «Расх» — расходы

Еженедельные строки:
- B = ЗП Иван, D = ЗП подмена, F = Нужды магазина, H = Закуп/АРЕНДА, J = Михаил, L = Дивиденды (подпись месяца выплаты в K).
- Строка 1404 «Всего»: `B1404=SUM(B2:B1403)` и т.д. по каждой колонке.
- `O2 = SUM(B1404,F1404,H1404,D1404,J1404,L1404)` — общая сумма всех расходов.
- Месячные блоки: N (месяц) / O (сумма) и Q (месяц) / R (сумма) — справочные.
- Проверочные формулы вида `B1463 = B1404 − SUM(B1406:B1462)`.

Важно: значения дивидендов (L) в Расх совпадают с «на выплату» (L) из вкладки «Расчет» — это связующее звено между книгами.

Вывод для приложения: вести журнал расходов по категориям с месячной агрегацией; месячная сумма расходов уходит в «Расчет».

---

## 5. Вкладка «Комиссии» — банковские комиссии

- Блоки по месяцам; внутри — по дням: «число» (A/F), эквайринг (B/G), QR (C/H), примечание (E/J).
- Итоги месяца: `B36=SUM(B4:B34)`, `E36=C36+D36`, `J36=G36+H36+I36`, позже `F= B+C+D+E` (с учётом подписки).
- Отдельные статьи: подписка «СберБизнес Прайм» (1 190 ₽/мес), банковские сборы.

Источник в банковской выписке `kl_to_1c`: строки «Зачисление средств по операциям эквайринга… Комиссия X (в т.ч. НДС Y)».

Вывод для приложения: из API банка тянуть комиссии по дням и мерчантам; месячная сумма комиссий учитывается в расходах/доходе.

---

## 6. Вкладка «Расчет» — главная формула (распределение прибыли)

Колонки: A=период, B=дата подсчёта, C=доход, D=зп Иван, E=зп подмена, F=расходы, G=аренда, H=на налог, I=обратно в оборот, J=итого, K=в оборот расходы, L=на выплату, M…T=доли участников.

**Основная формула (варианты по периодам):**
```
J (итого) = C (доход) − D (зп Иван) − E (зп подмена) − F (расходы) − G (аренда) − H (налог) − I (обратно в оборот) − K (в оборот расходы)
```
Примеры из книги: `J2 = C-D-E-F-G`, `J6 = C-D-E-F-G-I`, `J30 = C-D-E-F-G-H-I`, `J58 = C-D-E-F-G-H-I-K`.

**L (на выплату)** = округление J (вручную/значениями; в разные годы: округление вниз до целого, позже — до сотен).

**Доли (M:T) — итого 100 % от L:**
| Участник | Доля | Формула |
| --- | --- | --- |
| Миша | 35 % | `M = L×0.35` (некоторые месяцы −600/−800/−3000 ручной корректив) |
| Иван | 19,30 % | `N = L×0.193` |
| Сеня | 12,90 % | `O = L×0.129` |
| Сергей Б | 9,60 % | `P = L×0.096` |
| Никита | 9,60 % | `Q = L×0.096` |
| Андрей | 9,60 % | `R = L×0.096` |
| Даня | 2 % | `S = L×0.02` |
| Александр | 2 % | `T = L×0.02` |

Итог долей: 35+19,3+12,9+9,6×3+2×2 = 100 % ✓

Открытый вопрос: **откуда берётся «доход» (C)?** Это не сумма продаж (оборот ~589 тыс. при доходе ~230 тыс. за март 2022, маржа ≈ 39 %). Скорее всего — отдельная цифра из 1С или ручная оценка прибыли от реализации. Нужно уточнить у владельца (см. вопросы в конце).

---

## 7. Вкладка «Отчеты» — сводка

- C = Оборот (месяц) = сумма продаж из «Продажи» (проверено: март 2022 = 589 370,25 ✓).
- D = Доход (ручная цифра, совпадает с C в «Расчет»).
- F = дней в месяце; **G = C/F** (средняя выручка в день).
- **H = D/C×100** (маржа, %).
- `C65 = SUM('Продажи'!G1)` — общий оборот из вкладки Продажи.
- `C59 = C65 − SUM(C2:C58)` — остаток/сверка текущего месяца.
- K4 = `C65 − 'Расх'!O2 + O4` — связь с общими расходами из «Расх» («остаток общий»).

---

## 8. Вкладка «ЗП» — зарплата продавцов

- Список дней (A) и персональных продаж (B, C).
- `F2 = COUNT(B2:B549)` — всего дней работы.
- `G2 = F2 − H2` — дни Ивана, `H2 = COUNT(C2:C549)` — дни подмены.
- **ЗП = дни × 1 000 ₽ + 5 % × личные продажи:**
  - Иван: `H8 = (G2*1000) + (SUM(B2:B549)*0.05)`
  - Подмена: `H14 = (H2*1000) + (SUM(C2:C549)*0.05)`
- Колонка «Долг/выплата+» (H15 = J8) — сальдо.

---

## 9. Служебные вкладки

- **баланс говорово**: шаблон учёта взаимных долгов (Говорово ↔ Чистякова 2), без данных.
- **Справочник контрагентов**: 45 поставщиков (вейп/табак-дистрибьюторы) — справочник для поля «Поставщик».

---

## 10. Что это значит для приложения

Приложение должно, получая данные по API, автоматически считать:

1. **Продажи:** ежедневные суммы → оборот за месяц/период.
2. **Кассовый отчёт:** остаток на конец дня = начало + приходы − расходы; месячные суммы.
3. **Расходы:** журнал с категориями → месячная сумма.
4. **Комиссии:** из банка по дням/мерчантам → месячная сумма.
5. **Расчет (главное):** J = доход − зп − расходы − аренда − налог − обратно в оборот; L = округление; доли M…T по процентам.
6. **Отчеты:** оборот, среднее в день, маржа %.
7. **ЗП:** дни × 1000 + 5% × личные продажи.

---

## 11. Открытые вопросы (нужно уточнить)

1. **Источник «дохода» (C в Расчет)**: 1С? Расчёт по себестоимости? Ручной ввод?
2. **Какой API даёт данные**: 1С (HTTP-сервис / OData), банк (Сбербанк Бизнес API), кассовое ПО (АТОЛ/1С-касса), or комбинированный?
3. **Платформа приложения**: веб (браузер), десктоп, мобильное? Бэкенд на чём (Node/Python/...)?
4. Сколько магазинов/счетов: один «Чистякова 2» или несколько (есть «Говорово»)?
5. Доли участников и их изменение — статические (текущие) или нужно хранить историю изменений?
